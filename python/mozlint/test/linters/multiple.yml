---
StringLinter:
    description: >-
        Make sure the string foobar never appears in browser js
        files because it is bad
    rule: no-foobar
    extensions: ['.js']
    type: string
    payload: foobar

---
RegexLinter:
    description: >-
        Make sure the string foobar never appears in browser js
        files because it is bad
    rule: no-foobar
    extensions: ['.js']
    type: regex
    payload: foobar
