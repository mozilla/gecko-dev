import { Locale } from "icu4x"
import { Time } from "icu4x"
import { TimeFormatter } from "icu4x"
export function format(timeFormatterLocaleName: string, timeFormatterLength: DateTimeLength, timeFormatterTimePrecision: TimePrecision, timeFormatterAlignment: DateTimeAlignment, timeHour: number, timeMinute: number, timeSecond: number, timeSubsecond: number);
