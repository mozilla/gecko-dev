import { Calendar } from "icu4x"
import { Date } from "icu4x"
export function monthCode(dateYear: number, dateMonth: number, dateDay: number, dateCalendarKind: CalendarKind);
export function era(dateYear: number, dateMonth: number, dateDay: number, dateCalendarKind: CalendarKind);
