[DEFAULT]
head = "head_addons.js head_unpack.js"
firefox-appdir = "browser"
skip-if = ["os == 'android'"]
dupe-manifest = true
tags = "addons"

["test_filepointer.js"]
skip-if = [
  "require_signing",
]

["test_webextension_paths.js"]
tags = "webextensions"
