[css-transition-cross-document.html]
  expected:
    if not fission and debug and not sessionHistoryInParent: TIMEOUT
    if fission and not debug: TIMEOUT
    [TIMEOUT, OK]
  [Moving a transition across documents should reset its state]
    expected:
      if not fission and debug and not sessionHistoryInParent: TIMEOUT
      if fission and not debug: TIMEOUT
      [TIMEOUT, FAIL]
