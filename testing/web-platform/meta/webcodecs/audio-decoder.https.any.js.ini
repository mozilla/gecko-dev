[audio-decoder.https.any.html]
  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Vorbis without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Vorbis with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Flac without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Flac with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Vorbis without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Vorbis with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Flac without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Flac with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]


[audio-decoder.https.any.worker.html]
  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Vorbis without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Vorbis with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Flac without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.isConfigSupported() rejects invalid config: Flac with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Vorbis without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Vorbis with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Flac without description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]

  [Test that AudioDecoder.configure() rejects invalid config: Flac with empty description]
    expected:
      if (os == "mac") and not debug and (version == "OS X 10.15.7"): [PASS, FAIL]
