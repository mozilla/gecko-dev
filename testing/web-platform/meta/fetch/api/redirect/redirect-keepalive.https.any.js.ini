[redirect-keepalive.https.any.html]
  expected:
    if not tsan and (os == "linux") and (processor == "x86_64") and not debug and not asan: [OK, TIMEOUT]
    if not tsan and (os == "mac") and debug: [OK, TIMEOUT]
    if not tsan and (os == "linux") and (processor == "x86"): [OK, TIMEOUT]
    if tsan: [OK, TIMEOUT]
  [[keepalive\][iframe\][load\] mixed content redirect; setting up]
    expected:
      if not tsan and (os == "linux") and (processor == "x86_64") and not debug and not asan: [PASS, TIMEOUT]
      if not tsan and (os == "mac") and debug: [PASS, TIMEOUT]
      if not tsan and (os == "linux") and (processor == "x86"): [PASS, TIMEOUT]
      if tsan: [PASS, TIMEOUT]
