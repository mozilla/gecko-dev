[vector-effect-invalid.html]
  [e.style['vector-effect'\] = "non-scaling-stroke viewport" should not set the property value]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
      if (os == "android") and not debug: [PASS, FAIL]

  [e.style['vector-effect'\] = "non-scaling-stroke screen" should not set the property value]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
      if (os == "android") and not debug: [PASS, FAIL]
