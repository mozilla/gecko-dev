[pattern-external-reference.svg]
  expected:
    if not swgl and not tsan and (os == "win") and debug and (processor == "x86_64"): [PASS, FAIL]
    if swgl and fission and (os == "linux"): [PASS, FAIL]
    if not swgl and tsan: [PASS, FAIL]
