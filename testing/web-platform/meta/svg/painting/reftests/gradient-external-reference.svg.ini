[gradient-external-reference.svg]
  expected:
    if debug and (os == "linux") and fission and swgl: [FAIL, PASS]
    if debug and (os == "linux") and fission and not swgl: [PASS, FAIL]
    if debug and (os == "linux") and not fission and swgl: [PASS, FAIL]
    if debug and (os == "win") and (processor == "x86"): [PASS, FAIL]
    if not debug and tsan: [PASS, FAIL]
