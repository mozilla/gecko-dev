[SVGGeometryElement.getTotalLength-01.svg]
  prefs: [dom.svg.pathSegment.enabled:true]
  [SVGGeometryElement.prototype.getTotalLength(), getTotalLength - rect in document]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
      if (os == "android") and not debug: [PASS, FAIL]

  [SVGGeometryElement.prototype.getTotalLength(), getTotalLength - rect in document with percent units]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
      if (os == "android") and not debug: [PASS, FAIL]

  [SVGGeometryElement.prototype.getTotalLength(), getTotalLength - path modified with setPathData]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
      if (os == "android") and not debug: [PASS, FAIL]
