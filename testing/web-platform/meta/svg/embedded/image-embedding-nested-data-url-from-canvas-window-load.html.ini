[image-embedding-nested-data-url-from-canvas-window-load.html]
  expected:
    if not asan and not tsan and (os == "linux") and not debug: [PASS, FAIL]
    if not asan and tsan: [PASS, FAIL]
    if asan: [PASS, FAIL]
