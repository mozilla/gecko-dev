[wait.py]
  expected:
    if os == "android": TIMEOUT
  [test_beforeunload_prompt[capabilities0-none\]]
    expected:
      if not debug and (os == "mac") and (version == "OS X 14.7.1"): [FAIL, PASS]
      if not debug and (os == "mac") and (version == "OS X 14.7"): [FAIL, PASS]
      if debug and fission: PASS
      if debug and not fission: [FAIL, PASS]
      [PASS, FAIL]
