[sharedworker-in-worker.html]
  expected:
    if not tsan and (processor == "x86_64") and (os == "linux") and not debug and not asan: [OK, ERROR]
    if not tsan and (processor == "x86") and not debug: [OK, ERROR]
    if tsan: [OK, ERROR]
  [Base URL in workers: new SharedWorker()]
    expected: FAIL
