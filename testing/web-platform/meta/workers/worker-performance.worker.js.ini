[worker-performance.worker.html]
  [Resource timing seems to work in workers]
    expected:
      if not asan and (processor == "x86") and (os == "linux"): [PASS, FAIL]
      if asan and fission: [PASS, FAIL]
