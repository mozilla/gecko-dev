[fedcm-multi-idp-abort.https.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Test that the abort signal works when multiple IDPs are used.]
    expected:
      if os == "android": FAIL
      TIMEOUT
