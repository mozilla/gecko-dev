[fedcm-auto-selected-flag.https.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Test that the is_auto_selected bit is properly sent.]
    expected:
      if os == "android": FAIL
      TIMEOUT
