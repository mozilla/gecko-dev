[fedcm-pending-disconnect.https.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Test that disconnect can be pending when the test finishes.]
    expected:
      if os == "android": FAIL
      TIMEOUT
