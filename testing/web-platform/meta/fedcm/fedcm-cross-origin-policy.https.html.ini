[fedcm-cross-origin-policy.https.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Test that COEP policy do not apply to FedCM requests]
    expected:
      if os == "android": FAIL
      TIMEOUT
