[fedcm-continue-on.https.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [continue_on and IdentityProvider.resolve work correctly.]
    expected:
      if os == "android": FAIL
      TIMEOUT
