[resolve-after-preventsilentaccess.https.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Test that resolve clears the preventSilentAccess state.]
    expected:
      if os == "android": FAIL
      TIMEOUT
