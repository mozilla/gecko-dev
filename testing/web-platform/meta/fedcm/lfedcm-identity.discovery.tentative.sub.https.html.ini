[lfedcm-identity.discovery.tentative.sub.https.html]
  expected:
    if fission and (os == "win") and not debug and (processor == "x86_64"): TIMEOUT
    if fission and (os == "linux") and not asan and not tsan: [TIMEOUT, OK]
    if not fission: OK
    [OK, TIMEOUT]
  [Cross-origin identity credential discovery does not resolve with effective store from the wrong origin]
    expected:
      if (os == "linux") and fission and not asan and tsan: [PASS, TIMEOUT]
      if (os == "linux") and fission and not asan and not tsan: [NOTRUN, PASS, TIMEOUT]
      if (os == "win") and (processor == "x86_64") and not debug: [NOTRUN, TIMEOUT]
      if (os == "win") and (processor == "x86") and debug: [PASS, NOTRUN]
      if (os == "win") and (processor == "x86") and not debug: [PASS, NOTRUN]
      if (os == "mac") and debug: [PASS, TIMEOUT]
      if (os == "linux") and not fission: PASS
      [PASS, TIMEOUT, NOTRUN]

  [Cross-origin identity credential discovery does not resolve with ineffective store from the right origin]
    expected:
      if (os == "linux") and fission and not asan and tsan: PASS
      if (os == "linux") and fission and not asan and not tsan: [TIMEOUT, NOTRUN]
      if (os == "win") and not debug and (processor == "x86_64"): [TIMEOUT, PASS, NOTRUN]
      if (os == "mac") and debug: PASS
      if (os == "linux") and not fission: PASS
      [PASS, TIMEOUT]

  [Cross-origin identity credential discovery works using the effectiveQueryURL]
    expected:
      if (processor == "x86_64") and (os == "linux") and fission and not asan and not tsan: [PASS, TIMEOUT]
      if (processor == "x86_64") and (os == "win") and not debug: [PASS, TIMEOUT]
      if (processor == "x86") and not debug: [PASS, TIMEOUT]
