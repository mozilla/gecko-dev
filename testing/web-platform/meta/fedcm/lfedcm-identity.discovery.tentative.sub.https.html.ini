[lfedcm-identity.discovery.tentative.sub.https.html]
  expected:
    if (os == "linux") and fission and (processor == "x86_64") and not asan and tsan: [OK, TIMEOUT]
    if (os == "linux") and fission and (processor == "x86_64") and not asan and not tsan: [TIMEOUT, OK]
    if (os == "linux") and fission and (processor == "x86_64") and asan: [OK, TIMEOUT]
    if (os == "win") and not debug and (processor == "x86_64"): TIMEOUT
    if (os == "win") and not debug and (processor == "x86"): [TIMEOUT, OK]
    if (os == "linux") and fission and (processor == "x86"): [OK, TIMEOUT]
  [Cross-origin identity credential discovery does not resolve with effective store from the wrong origin]
    expected:
      if (os == "linux") and fission and (processor == "x86_64") and not asan and tsan: [PASS, TIMEOUT]
      if (os == "linux") and fission and (processor == "x86_64") and not asan and not tsan: [TIMEOUT, PASS]
      if (os == "linux") and fission and (processor == "x86_64") and asan: [PASS, TIMEOUT]
      if (os == "win") and not debug and (processor == "x86_64"): [NOTRUN, TIMEOUT]
      if (os == "win") and not debug and (processor == "x86"): [TIMEOUT, PASS, NOTRUN]
      if (os == "linux") and fission and (processor == "x86"): [PASS, TIMEOUT]

  [Cross-origin identity credential discovery does not resolve with ineffective store from the right origin]
    expected:
      if (os == "win") and not debug and (processor == "x86_64"): [TIMEOUT, PASS, NOTRUN]
      if (os == "win") and not debug and (processor == "x86"): [PASS, TIMEOUT, NOTRUN]

  [Cross-origin identity credential discovery works using the effectiveQueryURL]
    expected:
      if (os == "win") and not debug and (processor == "x86_64"): [PASS, TIMEOUT]
