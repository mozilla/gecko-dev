[callback-cross-realm-report-exception.html]
  [ResizeObserver reports the exception from its callback in the callback's global object]
    expected:
      if (os == "linux") and not tsan and not asan and fission and not debug and (processor == "x86_64"): [FAIL, PASS]
      if (os == "linux") and not tsan and asan and fission: [FAIL, PASS]
      if (os == "linux") and not tsan and not asan and not fission: [FAIL, PASS]
      if (os == "win") and debug and (processor == "x86_64"): [FAIL, PASS]
      if (os == "win") and debug and (processor == "x86"): [FAIL, PASS]
      if (os == "linux") and tsan: [FAIL, PASS]
      [PASS, FAIL]
