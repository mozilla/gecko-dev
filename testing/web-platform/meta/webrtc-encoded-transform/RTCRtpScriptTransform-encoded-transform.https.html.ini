[RTCRtpScriptTransform-encoded-transform.https.html]
  expected:
    if not debug and (processor == "x86") and (os == "win"): OK
    if not debug and (processor == "x86_64"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Receiver and sender read, modifiy and write video frames.]
    expected:
      if (os == "mac") and not debug: [FAIL, TIMEOUT]
      FAIL

  [Receiver and sender read, modifiy and write audio frames.]
    expected:
      if (os == "linux") and not debug and (processor == "x86_64"): TIMEOUT
      if (os == "mac") and not debug: [FAIL, NOTRUN]
      FAIL

  [The sender sends both a video frame it doesn't own and one it does. Receiver only gets the frame owned by the sender.]
    expected:
      if (os == "linux") and (processor == "x86_64") and not debug and asan: [TIMEOUT, PASS]
      if (os == "linux") and (processor == "x86_64") and not debug and not asan: [NOTRUN, PASS, TIMEOUT]
      if (os == "win") and (processor == "x86_64") and debug: [PASS, TIMEOUT]
      if (os == "win") and (processor == "x86_64") and not debug: [TIMEOUT, PASS]
      if (os == "mac") and not debug: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and (processor == "x86"): [PASS, TIMEOUT, NOTRUN]
      if (os == "android"): [NOTRUN, TIMEOUT, PASS]

  [The sender sends both an audio frame it doesn't own and one it does. Receiver only gets the frame owned by the sender.]
    expected:
      if (os == "win") and (processor == "x86_64") and debug: [PASS, TIMEOUT, NOTRUN]
      if (os == "win") and (processor == "x86_64") and not debug: [TIMEOUT, PASS, NOTRUN]
      if (os == "linux") and (processor == "x86_64") and not debug: [NOTRUN, PASS, TIMEOUT]
      if (os == "mac") and not debug: [NOTRUN, PASS]
      if (os == "android"): [NOTRUN, TIMEOUT, PASS]

  [Sender writes video frames in incorrect order. Frames written in incorrect order are discarded.]
    expected:
      if (os == "win") and (processor == "x86_64") and debug: [FAIL, TIMEOUT, NOTRUN]
      if (os == "win") and (processor == "x86_64") and not debug: [NOTRUN, FAIL, TIMEOUT]
      if (os == "linux") and not debug and (processor == "x86_64"): [NOTRUN, FAIL]
      if (os == "linux") and not debug and (processor == "x86"): [FAIL, NOTRUN]
      if (os == "mac") and not debug: [NOTRUN, FAIL, TIMEOUT]
      if (os == "android"): [NOTRUN, FAIL, PASS, TIMEOUT]
      FAIL

  [Sender writes audio frames in incorrect order. Frames written in incorrect order are discarded.]
    expected:
      if not debug and (os == "win") and (processor == "x86"): FAIL
      if not debug and (os == "linux") and (processor == "x86"): [FAIL, NOTRUN]
      if not debug and (os == "mac"): [NOTRUN, PASS]
      if debug: [FAIL, NOTRUN]
      [NOTRUN, FAIL]

  [Sender reads frames but doesn't write them back. Receiver doesn't receive any frames.]
    expected:
      if (os == "linux") and not debug and (processor == "x86_64"): [NOTRUN, PASS]
      if (os == "linux") and not debug and (processor == "x86"): [PASS, NOTRUN]
      if (os == "mac") and not debug: [PASS, NOTRUN]

  [Sender skips some frames and only writes one back. Receiver only receives that frame.]
    expected:
      if (os == "linux") and not debug and (processor == "x86_64"): [NOTRUN, PASS]
      if (os == "linux") and not debug and (processor == "x86"): [PASS, NOTRUN]
      if (os == "mac") and not debug: [PASS, TIMEOUT, NOTRUN]
