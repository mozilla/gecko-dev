[RTCRtpScriptTransform-encoded-transform.https.html]
  expected:
    if (os == "win") and not debug and (processor == "x86_64"): [TIMEOUT, OK]
    if (os == "linux") and debug and not fission: [OK, TIMEOUT]
    if (os == "linux") and not debug: [TIMEOUT, OK]
    if (os == "android") and debug: TIMEOUT
    if (os == "android") and not debug: [TIMEOUT, OK]
  [Receiver and sender read, modifiy and write video frames.]
    expected: FAIL

  [Receiver and sender read, modifiy and write audio frames.]
    expected: FAIL

  [The sender sends both a video frame it doesn't own and one it does. Receiver only gets the frame owned by the sender.]
    expected:
      if (processor == "x86_64") and debug and not fission and (os == "linux"): [PASS, TIMEOUT]
      if (processor == "x86_64") and debug and fission: PASS
      if (processor == "x86_64") and not debug and (os == "win"): [PASS, TIMEOUT]
      if processor == "x86": PASS
      [TIMEOUT, PASS]

  [The sender sends both an audio frame it doesn't own and one it does. Receiver only gets the frame owned by the sender.]
    expected:
      if (os == "win") and not debug and (processor == "x86_64"): [PASS, TIMEOUT, NOTRUN]
      if (os == "linux") and debug and not fission: [PASS, NOTRUN]
      if (os == "linux") and not debug: [NOTRUN, PASS]
      if (os == "android") and debug: [NOTRUN, TIMEOUT]
      if (os == "android") and not debug: [NOTRUN, PASS, TIMEOUT]
