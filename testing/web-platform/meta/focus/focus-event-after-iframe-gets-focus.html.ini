[focus-event-after-iframe-gets-focus.html]
  expected:
    if not fission and (os == "linux") and debug: [TIMEOUT, OK, CRASH]
  [Check focus event after innermost different site iframe gets focus]
    expected:
      if not fission and (os == "linux") and debug: TIMEOUT
