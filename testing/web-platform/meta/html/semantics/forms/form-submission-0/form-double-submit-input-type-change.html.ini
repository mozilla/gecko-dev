[form-double-submit-input-type-change.html]
  [default submit action should supersede input onclick submit() and change the input type from radio to submit]
    expected:
      if (os == "mac") and not debug: [FAIL, PASS]
      FAIL
