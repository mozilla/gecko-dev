[localstorage-about-blank-3P-iframe-opens-3P-window.partitioned.html]
  expected:
    if fission and (os == "win") and debug and (processor == "x86_64"): [OK, TIMEOUT]
    if fission and (os == "mac") and debug: [OK, TIMEOUT]
    if fission and (os == "linux"): [OK, TIMEOUT]
  [StorageKey: test 3P about:blank window opened from a 3P iframe]
    expected:
      if (os == "win") and debug and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "mac") and debug: [PASS, TIMEOUT]
      if (os == "linux") and fission: [PASS, TIMEOUT]
      if os == "android": FAIL
