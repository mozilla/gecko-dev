[localstorage-about-blank-3P-iframe-opens-3P-window.partitioned.html]
  expected:
    if fission and (processor == "x86_64") and (os == "win") and debug: [OK, TIMEOUT]
    if fission and (processor == "x86_64") and (os == "linux") and debug: [OK, TIMEOUT]
    if fission and (processor == "x86_64") and (os == "linux") and not debug: [TIMEOUT, OK]
  [StorageKey: test 3P about:blank window opened from a 3P iframe]
    expected:
      if (os == "win") and debug and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "linux") and fission and debug: [PASS, TIMEOUT]
      if (os == "linux") and fission and not debug: [TIMEOUT, PASS]
      if os == "android": FAIL
