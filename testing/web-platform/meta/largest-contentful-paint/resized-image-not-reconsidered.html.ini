[resized-image-not-reconsidered.html]
  [Resized image should not be reconsidered as LCP]
    expected:
      if not asan and (os == "linux") and (processor == "x86_64") and not debug and not tsan: [PASS, FAIL]
      if not asan and (os == "linux") and (processor == "x86"): [PASS, FAIL]
      if asan and fission: [PASS, FAIL]
      if asan and not fission: [FAIL, PASS]
