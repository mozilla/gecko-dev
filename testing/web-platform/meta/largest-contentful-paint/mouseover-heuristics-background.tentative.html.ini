[mouseover-heuristics-background.tentative.html]
  expected:
    if (os == "win") and not debug and (processor == "x86"): [ERROR, OK, TIMEOUT]
    if (os == "linux") and debug and fission: [TIMEOUT, OK, ERROR]
    if (os == "linux") and debug and not fission: [ERROR, OK, TIMEOUT]
    if (os == "linux") and not debug and fission: [ERROR, OK, TIMEOUT]
    if (os == "linux") and not debug and not fission: [ERROR, OK, TIMEOUT]
    [ERROR, OK]
  [LCP mouseover heuristics ignore background-based zoom widgets]
    expected:
      if (os == "android") and not debug: TIMEOUT
      if os == "win": TIMEOUT
      if os == "mac": TIMEOUT
      [TIMEOUT, PASS]
