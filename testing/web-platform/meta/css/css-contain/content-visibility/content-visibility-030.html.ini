[content-visibility-030.html]
  [IntersectionObserver interactions]
    expected:
      if (os == "linux") and not debug and not tsan and not fission: [PASS, FAIL]
      if (os == "linux") and not debug and tsan: [FAIL, PASS]
      if (os == "linux") and debug: [PASS, FAIL]
