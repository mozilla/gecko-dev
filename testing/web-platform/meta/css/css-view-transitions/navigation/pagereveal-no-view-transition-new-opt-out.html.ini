[pagereveal-no-view-transition-new-opt-out.html]
  expected:
    if (os == "linux") and not debug and asan and fission: [ERROR, TIMEOUT]
    if (os == "linux") and debug and fission: [ERROR, TIMEOUT]
    if (os == "linux") and debug and not fission: [ERROR, TIMEOUT]
    ERROR
  [\n  View transitions: null event.viewTransition when new page removes opt-in\n  before pagereveal\n]
    expected: TIMEOUT
