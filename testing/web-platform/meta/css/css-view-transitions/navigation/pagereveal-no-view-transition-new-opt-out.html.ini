[pagereveal-no-view-transition-new-opt-out.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not tsan and not debug and fission and asan: [ERROR, TIMEOUT]
    if (os == "linux") and (processor == "x86_64") and not tsan and debug and not fission: [ERROR, TIMEOUT]
    ERROR
  [\n  View transitions: null event.viewTransition when new page removes opt-in\n  before pagereveal\n]
    expected: TIMEOUT
