[pagereveal-no-view-transition-new-opt-out.html]
  expected:
    if (os == "linux") and not tsan and not debug and fission and asan: [ERROR, TIMEOUT]
    if (os == "linux") and not tsan and debug and not fission: [ERROR, TIMEOUT]
    ERROR
