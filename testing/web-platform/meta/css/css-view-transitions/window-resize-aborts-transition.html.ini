[window-resize-aborts-transition.html]
  expected:
    if os == "android": TIMEOUT
    if os == "linux" and os_version == "24.04" and processor == "x86_64" and display == "x11": [OK, TIMEOUT]
  [View transitions: Resizing viewport skips the transition]
    expected:
      if os == "android": TIMEOUT
      if os == "linux" and os_version == "24.04" and processor == "x86_64" and display == "x11": [PASS, TIMEOUT]
