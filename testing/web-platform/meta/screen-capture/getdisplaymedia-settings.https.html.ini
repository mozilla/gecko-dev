[getdisplaymedia-settings.https.html]
  [getDisplayMedia() deviceId setting and capability]
    expected: FAIL

  [getDisplayMedia() and facingMode]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
      if os == "android": FAIL
