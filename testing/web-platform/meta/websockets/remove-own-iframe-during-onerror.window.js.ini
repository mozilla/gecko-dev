[remove-own-iframe-during-onerror.window.html?wss]

[remove-own-iframe-during-onerror.window.html?wpt_flags=h2]
  expected:
    if (os == "linux") and (processor == "x86_64") and debug and fission: [OK, TIMEOUT]
    if (os == "linux") and (processor == "x86_64") and debug and not fission: [OK, TIMEOUT]
    if (os == "linux") and (processor == "x86_64") and not debug: [OK, TIMEOUT]
    if os == "android": [OK, ERROR, TIMEOUT]

[remove-own-iframe-during-onerror.window.html?default]
