[scroll-margin-non-scrolling-root.html]
  [Test scroll margin intersection]
    expected:
      if (os == "linux") and not tsan and not debug and asan and fission: [FAIL, PASS]
      if (os == "linux") and not tsan and debug and fission: PASS
      if (os == "android") and debug: PASS
      if os == "win": PASS
      if os == "mac": PASS
      [PASS, FAIL]
