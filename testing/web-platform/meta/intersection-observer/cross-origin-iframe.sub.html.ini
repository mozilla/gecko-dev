[cross-origin-iframe.sub.html]
  [First rAF]
    expected:
      if not fission and debug and (os == "android"): PASS
      if fission: [FAIL, PASS]
      [PASS, FAIL]

  [topDocument.scrollingElement.scrollTop = 100]
    expected:
      if fission: FAIL

  [iframeDocument.scrollingElement.scrollTop = 250]
    expected:
      if fission and (os == "linux") and not asan and not debug and (processor == "x86_64") and not tsan: [FAIL, PASS]
      if fission and (os == "win") and (processor == "x86_64"): PASS
      if fission and (os == "mac") and debug: PASS
      if not fission: PASS
      [PASS, FAIL]

  [topDocument.scrollingElement.scrollTop = 200]
    expected:
      if (os == "linux") and fission and not asan and not debug and (processor == "x86_64"): [FAIL, PASS]
      if (os == "linux") and fission and asan: [FAIL, PASS]
      if (os == "mac") and debug: PASS
      if (os == "linux") and not fission: PASS
      if os == "android": PASS
      [PASS, FAIL]
