[partitioned-popins.proxy-cross.tentative.sub.https.window.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not debug and not tsan and fission and not asan: [OK, TIMEOUT]
    if (os == "linux") and (processor == "x86_64") and not debug and not tsan and not fission: [OK, TIMEOUT]
    if (os == "linux") and (processor == "x86"): [OK, TIMEOUT]
  [Verify cross-site Partitioned Popins proxies only have access to postMessage and closed methods.]
    expected: FAIL
