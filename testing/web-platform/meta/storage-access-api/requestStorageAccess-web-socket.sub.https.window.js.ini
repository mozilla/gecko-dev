[requestStorageAccess-web-socket.sub.https.window.html]
  [WebSocket omits unpartitioned cookies without storage access]
    expected:
      if os == "android": [FAIL, PASS]
      if (os == "linux") and debug and release_or_beta: [FAIL, PASS]
      if release_or_beta: FAIL # Bug 19489465
