[requestStorageAccess-cross-site-fetch.sub.https.window.html]
  [Cross-site HTTP redirects from a frame with storage-access are not credentialed by default]
    expected:
      if os == "android": PASS
      if release_or_beta: PASS
      FAIL
