[fullscreen-root-block-scroll.html]
  expected:
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [fullscreen root block scrolling]
    expected:
      if (os == "mac") and not debug: [PASS, TIMEOUT]
