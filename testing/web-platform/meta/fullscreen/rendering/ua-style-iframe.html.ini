[ua-style-iframe.html]
  expected:
    if (os == "mac") and not debug: [OK, ERROR]
  [User-agent levels style sheet defaults for iframe]
    expected:
      if (os == "mac") and not debug: [PASS, FAIL]
