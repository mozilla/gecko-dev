[element-request-fullscreen-timing.html]
  expected:
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Timing of fullscreenchange and resize events]
    expected:
      if (os == "mac") and debug: [FAIL, PASS]
      if (os == "mac") and not debug: [PASS, FAIL, TIMEOUT]
      if os == "android": FAIL

  [Timing of fullscreenerror event]
    expected:
      if fission and not tsan and debug and (os == "win") and (processor == "x86_64"): [PASS, FAIL]
      if fission and not tsan and debug and (os == "mac"): [PASS, FAIL]
      if not fission and (os == "linux") and debug: [PASS, FAIL]
      if fission and tsan: [PASS, FAIL]
