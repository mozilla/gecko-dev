[element-request-fullscreen-timing.html]
  expected:
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Timing of fullscreenchange and resize events]
    expected:
      if (os == "mac") and debug: [PASS, FAIL]
      if (os == "mac") and not debug: [PASS, FAIL, TIMEOUT]
      if os == "android": FAIL

  [Timing of fullscreenerror event]
    expected:
      if debug and (processor == "x86_64") and (os == "linux") and fission: PASS
      if debug and (processor == "x86_64") and (os == "android"): PASS
      if not debug and tsan: [FAIL, PASS]
      if not debug and not tsan: PASS
      [PASS, FAIL]
