[element-request-fullscreen-timing.html]
  expected:
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Timing of fullscreenchange and resize events]
    expected:
      if (os == "linux") and not asan and fission and not tsan and not debug and (processor == "x86"): [PASS, FAIL]
      if (os == "linux") and not asan and fission and not tsan and debug: [PASS, FAIL]
      if (os == "linux") and not asan and fission and tsan: FAIL
      if (os == "win") and (processor == "x86") and not debug: [PASS, FAIL]
      if (os == "linux") and asan and not fission: FAIL
      if (os == "win") and (processor == "x86_64"): [PASS, FAIL]
      if (os == "mac") and not debug: [PASS, FAIL, TIMEOUT]
      [FAIL, PASS]

  [Timing of fullscreenerror event]
    expected:
      if not debug and not asan and not tsan: PASS
      if debug and (os == "mac"): [FAIL, PASS]
      [PASS, FAIL]
