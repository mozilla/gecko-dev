[document-exit-fullscreen-timing.html]
  expected:
    if (os == "mac") and debug: CRASH
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Timing of fullscreenchange and resize events]
    expected:
      if os == "mac": [PASS, TIMEOUT]
