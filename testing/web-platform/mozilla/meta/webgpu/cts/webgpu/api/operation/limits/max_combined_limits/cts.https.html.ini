[cts.https.html?q=webgpu:api,operation,limits,max_combined_limits:max_storage_buffer_texture_frag_outputs:*]
  implementation-status:
    if os == "mac": backlog
  [:format="r8uint"]
    expected:
      if os == "mac": FAIL

  [:format="rgba32uint"]
    expected:
      if os == "mac": FAIL

  [:format="rgba8uint"]
    expected:
      if os == "mac": FAIL
