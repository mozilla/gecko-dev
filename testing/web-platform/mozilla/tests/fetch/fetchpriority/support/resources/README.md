This directory contains some resources loaded by fetchpriority tests.

* `generate_fonts.py` is used to generate `font_*.woff2` files.
* `generate_media_files.py` is used to generate `audio_*.opus` and
  `video_*.webm` files.
