diff --git a/expat/lib/expat_external.h b/expat/lib/expat_external.h
--- a/expat/lib/expat_external.h	2024-09-10 13:50:16
+++ b/expat/lib/expat_external.h	2024-09-12 17:03:49
@@ -137,6 +137,9 @@
 #  endif
 #endif
 
+/* BEGIN MOZILLA CHANGE (typedef XML_Char to char16_t) */
+#if 0
+
 #ifdef XML_UNICODE /* Information is UTF-16 encoded. */
 #  ifdef XML_UNICODE_WCHAR_T
 typedef wchar_t XML_Char;
@@ -149,6 +152,9 @@
 typedef char XML_Char;
 typedef char XML_LChar;
 #endif   /* XML_UNICODE */
+
+#endif
+/* END MOZILLA CHANGE */
 
 #ifdef XML_LARGE_SIZE /* Use large integers for file/stream positions. */
 typedef long long XML_Index;
diff --git a/expat/lib/xmlparse.c b/expat/lib/xmlparse.c
--- a/expat/lib/xmlparse.c	2024-09-10 18:28:14
+++ b/expat/lib/xmlparse.c	2024-09-12 17:04:06
@@ -193,6 +193,9 @@
 
 #endif
 
+/* BEGIN MOZILLA CHANGE (typedef XML_Char to char16_t) */
+#if 0
+
 #ifdef XML_UNICODE
 
 #  ifdef XML_UNICODE_WCHAR_T
@@ -209,6 +212,9 @@
 #  define XML_L(x) x
 
 #endif
+
+#endif
+/* END MOZILLA CHANGE */
 
 /* Round up n to be a multiple of sz, where sz is a power of 2. */
 #define ROUND_UP(n, sz) (((n) + ((sz) - 1)) & ~((sz) - 1))
