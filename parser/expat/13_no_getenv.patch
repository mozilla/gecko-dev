diff --git a/expat/lib/xmlparse.c b/expat/lib/xmlparse.c
--- a/expat/lib/xmlparse.c
+++ b/expat/lib/xmlparse.c
@@ -8782,6 +8782,8 @@ unsignedCharToPrintable(unsigned char c)
 
 static unsigned long
 getDebugLevel(const char *variableName, unsigned long defaultDebugLevel) {
+/* BEGIN MOZILLA CHANGE (don't use environment values) */
+#if 0
   const char *const valueOrNull = getenv(variableName);
   if (valueOrNull == NULL) {
     return defaultDebugLevel;
@@ -8797,4 +8799,8 @@ getDebugLevel(const char *variableName, 
   }
 
   return debugLevel;
-}
+#else
+  return 0;
+#endif
+/* END MOZILLA CHANGE (don't use environment values) */
+}
